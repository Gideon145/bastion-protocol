Bastion Protocol. Autonomous exploit detection for Robinhood Chain. Here is the pipeline live.

The healthcheck endpoint shows the agent running 24/7 on Railway. Current cycles, FSM state, and threat score updating every 15 seconds. Two smart contracts deployed on Robinhood Chain.

Now watch the logs. The agent collects pending transactions from Alchemy WebSocket. Scores every block using an 8-element feature vector. The FSM trips when the score crosses the threshold. Detection recorded on-chain. Transaction confirmed on Robinhood Chain.

And here is the Telegram alert. Real-time notification with the threat signature, severity, and transaction hash. Every detection traceable to on-chain proof.

Collect. Score. Detect. Attest. Alert. Bastion Protocol.
